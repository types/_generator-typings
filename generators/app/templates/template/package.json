{
  "private": true,
  "scripts": {
    "watch": "onchange '**/*.d.ts' -e 'out/**' -- npm run build",
    "build": "typings bundle<%- ambient %> -o out",
    "test": "ts-node -n test/test.ts"
  },
  "devDependencies": {
    "blue-tape": "^0.2.0",
    "is-callable": "1.1.3",
    "onchange": "^2.1.2",
    "ts-node": "^0.7.0",
    "tslint": "git+https://github.com/unional/tslint.git",
    "tslint-config-typings": "^0.1.2",
    "typescript": "^1.8.9",
    "typings": "^0.7.9"
  }
}